# BitGet API credentials
# Get these from: BitGet account → API Management
# Withdrawals: OFF. IP whitelist: ON.
BITGET_API_KEY=your_api_key_here
BITGET_SECRET_KEY=your_secret_key_here
BITGET_PASSPHRASE=your_passphrase_here
BITGET_BASE_URL=https://api.bitget.com

# Trading mode: "spot" or "futures"
TRADE_MODE=spot

# Portfolio value — used to calculate position size
PORTFOLIO_VALUE_USD=1000

# Trade limits — these are your guardrails
MAX_TRADE_SIZE_USD=100        # No single trade can exceed this value
MAX_TRADES_PER_DAY=3          # Bot stops executing after this many trades in a calendar day

# Symbol and timeframe
SYMBOL=BTCUSDT
TIMEFRAME=5m

# Set to "true" to run in paper trading mode (logs decisions but never places real orders)
PAPER_TRADING=true

# ─── Web service / dashboard (Railway) ─────────────────────────────────────
# DATA_DIR  → mount path of the Railway Volume; lets trades.csv +
#             safety-check-log.json survive across runs. Leave empty locally.
# PORT      → Railway injects this automatically; leave empty locally.
# API_KEY   → optional. If set, /api/* require ?key=<API_KEY>. Off by default.
# CRON_SPEC → optional override for the internal scheduler (default every 5m).
DATA_DIR=
PORT=
API_KEY=
CRON_SPEC=*/5 * * * *
